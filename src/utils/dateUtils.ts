import { parseISO, format, differenceInDays, isValid, startOfDay } from 'date-fns'; export function formatDateDe(dateString: string | null | undefined): string { if (!dateString) { return 'Keine Angabe'; } const parsedDate = parseISO(dateString); if (!isValid(parsedDate)) { return 'Keine Angabe'; } return format(parsedDate, 'dd.MM.yyyy'); } export function getAmpelStatus(dateString: string | null | undefined): 'green' | 'yellow' | 'red' | 'none' { if (!dateString) { return 'none'; } const parsedDate = parseISO(dateString); if (!isValid(parsedDate)) { return 'none'; } const today = startOfDay(new Date()); const targetDate = startOfDay(parsedDate); const daysRemaining = differenceInDays(targetDate, today); if (daysRemaining < 0) { return 'red'; } if (daysRemaining <= 30) { return 'yellow'; } return 'green'; } export function getCurrentFormattedTime(): string { const now = new Date(); return format(now, 'dd.MM.yyyy, HH:mm'); }